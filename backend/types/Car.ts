export type Car = {
  make: string;
  model: string;
  trim: string;
  carType: string;
  fuelType: string;
  year: number;
  seats: number;
  pricePerDay: number;
  pickupLocation: string;
  dropoffLocation: string;
};
